#pragma once // Previene definiciones múltiples al incluir un archivo de encabezado
#include <iostream> // Proporciona acceso de entrada/salida estándar
#include <string> // Proporciona acceso a la clase de cadenas de char
#include <ctime>
#include <vector> // Proporciona acceso al contenedor de vectores
#include <SFML/Graphics.hpp> // Proporciona acceso a la biblioteca de gráficos SFML
#include <SFML/Audio.hpp> // Proporciona acceso a la biblioteca de audio SFML

using namespace std; // Permite utilizar las clases y funciones de la biblioteca estándar sin necesidad de usar prefijo std::
using namespace sf; // Permite utilizar las clases y funciones de la biblioteca SFML sin necesidad de usar prefijo sf::

